One tenant,onelake per organization,
One tnent -> many domains
one domain -> many workspaces
worspace -> data pipelines,all items (building blocks)
each item -> many objects


#### microsoft onelake

onelake ->serverless compute -> data consumption


EventStream windowing functions ->

tumbling window : fixed window like 5 min no overlap 
hopping window : fixed window but overlap ,like fix 5 min window and 2 min overlap -> uses for moving query
sliding window: dynamically triggered by incoming data ,new window open when event arrives ,window look back in time and forward for defined duration .ex 5 min
session window:not fixed window,based on period of continous activity
snapshot: processed events  that share the exact timestamp,no duration is defined 


data pipeline:

-> data movement  - copy data
- > data transofrmation -  dataflow,script,notebook,stored procedure
- > control- for each ,if condition,func,azure databricks
  

masking:

-> default()
-> email() ,show first charactera and mask rest with xxx,emil replaced with xxxx.com
-> random(1,1000) ex 1234 = 15 , 0=793


three type of endorsement:
promoted -> basically  highlight item that is seen as valualbel asset ,any item user can do it just to make it important
certified -> means item meet org qualtiy standard, can be regarded as reliable,authorative and ready for sue across organization ,fabric admin manage it
master data -> data item is rgarded by org as being core,single sourc eof truth dat  like customer list or product code

item sensitivty label:


  
