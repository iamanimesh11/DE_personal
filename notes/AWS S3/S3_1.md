<h2> Amazon S3 = Object Storage Service</h2>

It stores data as objects (not files or blocks).

Each object has:

- **Data** — the actual file  
- **Metadata** — file type, size, etc.  
- **Key** — unique name in the bucket  

### Example

s3://my-company-logs/2025/october/app_logs.json

- **Bucket**: `my-company-logs`  
- **Key (path)**: `2025/october/app_logs.json`

🧱 . How Object Storage Differs from Block/File Storage

| Type                    | Description                                         | Common Use                           |
| ----------------------- | --------------------------------------------------- | ------------------------------------ |
| **Block Storage**       | Splits data into fixed-size blocks (e.g., EBS).     | Databases, OS volumes                |
| **File Storage**        | Hierarchical (folders/files), supports locking.     | Shared drives (EFS, NAS)             |
| **Object Storage (S3)** | Flat structure, identified by key, highly scalable. | Backups, data lakes, logs, analytics |

💡 Think of S3 as a global file cabinet — infinite drawers, each storing an object with a label (key).

When you upload an object:
It’s automatically replicated across multiple Availability Zones (AZs) within the region.

S3 is for analytics (olap) ,data lakearchival systems.
-> S3  is for ETL staging area for airflow/spark before loading to db
-> serve static sites
-> store compressed backup ,logs,reports
-> collect log from multiple places and stored in s3 (log centralization)
-> for machine learning datset ,,model artifacts .

S3  terms:

bucket: -container for objects (top level)
object: data + metadata
key: uniqeu identifier (like file path)
prefix : common part of objec keys(liker folder)
region: physicall aws data centre location
versioning: keep multple versions of same object
storage classC: determine cost,acess frequencey,durability


design philosophy of s3:

petabytes or more data can be stored without worrying about managing disk.
comput like ec2,glue,lambda connect to s3 via api call -no direct mountinh

example: ec2/lambda read raw logs from s3 ->transform-> write back processed data->athena queries it


## if wants to upload an entire direcotry:
use recursive approach
aws s3 cp path  s3path --recursive


## presigned urls(temporary public access)

when want to share an object securely (like csv ) with someone who doesnt have access
example: generate a link to object for 1 hour

aws s3 presign s3_path --expires-in 3600

used BOTO3(aws sdk for python ) for pyhon autmation at script level
- useful for etl jobs ,airflow task scheduling ,daa ingestion pipelines

## bucket policyp-ACLs,bucket policies,IAM role and policies
S3 access control type:

control type  ,scope ,decription
Iam policy  -user/role    -it control who can access  which bucket 

bucket policy  - bucket   -json based policy applie ddirlty on bucket 

ACL  -object              - legaxy rarely used,gve access to specific aws accounts

blcok public accesss - bucket/account  glbal sield that overide all public settings (nobody on interet can see the data )


# secure bucket policy example


