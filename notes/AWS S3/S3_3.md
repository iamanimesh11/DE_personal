# S3 Security (Encryption + Policies + Conditions + ACLs)

1️⃣ SSE-S3 (AES-256)
2️⃣ SSE-KMS (Customer-managed keys)
3️⃣ SSE-C (Customer-provided keys)
4️⃣ Bucket Policy Advanced Conditions
5️⃣ ACLs (legacy but exam-critical)

S3 security exist to answer 3 basic question:
1.who can access my data
2. what can they do with it (read ,write ,delete)
3. how is data protected if someone steal it ?

High level view of S3 security:
1.Encryption -protect the data itself,even if someone steal data somehow but shouldnt be understandable.
encrypt data at rest(in s3),encrypt data in transit(while uploading .downlaoding) ,it slike locking data with a key

2.policies -who is allowed to that?make sure not everyone have full access. 
written rule(json) say who can access (user ,role,account) and what they can do(read write or delete) ,on which obhject or bucket is allowed

3. conditions -under what situation access is allowed
  sometime access should be allowe only in specfic cases like only from spcific IP,only using https,only from certain aws service,only during businness hours.
so access is allowed but base don spcific concdition

4 . ACLs, access control list 
its ourdated ,simple permission only neede dbefore policies were there to share object.
- control access at objec tlevel
- very basic (read/write)
- avoided in favor of policies

policies vs access control list comparison

feature | s3 policies | s3 ACLs
format |JSON  |   XML LIKE
GRANULARITY (means control )| VERY FINE GRDED (mor econtrol )| (lesss control)
CONDITION(IP,HTTPS,MFA) | YES| NO
IAM INTEGRATION | YES| NO
CROSS ACCOUNT ACCESS  | EASY | NO
AWS RECOMMENDATIONS  | PREFERRED | LEGACY

acl CAN WORK ON OBJECT LEVEL MOSTLY ,although policies works upfront


## 1️⃣ SSE-S3 (AES-256) (server side encryption -S3 managed key)

server sid encryption with amazon s3 managed keys
- uses AES256 ,encryption happen at rest,amazon s3 manages everything

key points:
encryption type is server side
key management -fully managed by s3 ,no need to see or control keys,its enabled by defult on new s3 buckets 
no extra cost
no KMS no iam key policies

so aws encrypt the uploaded object using aes-256 and decrypt when accessed 

SSE-S3 isnt good choice when mention:
customer managed keys,key roational control,audit key usage,compilance requriing key ownership  

custoemr has 0 control on thes keys

## 2️⃣ SSE-KMS (Server Side Encryption — AWS KMS keys)

server side encryption using aws key management service
- uses aes 256,still encryption at rest but can control keys

key points:
encryption type is server side
key management -KMS KEYS (aws managed or custoemr managed)
user control key permission and rotation,
key usage is logged in cloudtrail
extra cost (for kms api call)
require IAM +KMS permissions

contorl ,compilance and cost 

in case wen access is denice even thought s3 policy allow it ,missing picene is ofen kms key polic or iam permision
how it works??

user upload a file to amazon s3 with sse kms enabled
s3 request a data key from kms (s3 call aws key management servic eusing a kms key)
kms generate a data key ,it create plaaintext data key(temp) ,encrypted data key(enrypted by kms key) calls as envelop encryption'
s3 encrypt the object - object is encrypte dusing AES-256 ,use the plaintext data key and its immedialty discarded
s3 store encrypted object and ecrypted data key(metadata)

object retrieval ->
s3 send encrypted data key to kms 
kms decrypt it (if allowed)
s3 decrypt the object and return it


why we need sse-kms when  sse-e3 is already free?
for general purpose whwee compilance and audit,control  not reuqired as kms call api and it smake cost per call which can be expencisve at some point of time unless required
like data lake ingestinn,real tie upload so no need of extra kms calls here and bette performance

ss3-kms are basiclaly owned ,enabled/diabled ,pemission by user so its answerable in furture audit via logs.
so encryptio n without ke ownership is not real secuirty for compilance.
example credit card info,medicla record,financial reports .

case when developer has s3getobjecr tpermission but should not cess seneistive data ,si kms:Decryt permission can be removed and user cn see the object exist but cannot read it


3️⃣ SSE-C (Customer-provided keys)
its basically key provided by user directly  and aws encrypt using it but doesnt store the key ,no recovery if lose it
no cloudtrail key usage audit ,not supported by aws many services
cannot use with :S3 defualt encryption,crossregion replication

its used rarely when extremen security and zero trust ,when company policy forbid aws from even sotring encryption keys 
it must be pass with every request 

how it works?
generated a 256bit key locally ,when uploading objec tto s3 ,send the encrytion key ,key md5 hash
s3 encrypt the object using aes 256
encryption happen server side .

when downlading the object it requrie sme key agAin to decrypt 


in short,

SSE-S3:AWS OWN KEY
SS3-KMS :SHARED CONTORL
SSE-C :USER ONW EVERYTHING


🔐 Advanced S3 Bucket Policy Conditions (Exam-Focused)
Bucket policies = JSON + conditions
Conditions answer WHEN access is allowed or denied.

1️⃣ Enforce HTTPS only (🔥 MOST COMMON)

"Condition": {
  "Bool": {
    "aws:SecureTransport": "false"
  }
}

Meaning
❌ Deny all HTTP
✅ Allow only HTTPS

Exam clue> “Ensure data is accessed securely”

2️⃣ Restrict by IP Address
```json
"Condition": {
  "IpAddress": {
    "aws:SourceIp": "203.0.113.0/24"
  }
}
```
Meaning
Only allow requests from a specific IP range

Exam clue> “Only allow access from corporate network”

3️⃣ Enforce SSE-KMS encryption on upload
```json
"Condition": {
  "StringEquals": {
    "s3:x-amz-server-side-encryption": "aws:kms"
  }
}
```
Meaning :Users must use SSE-KMS
Rejects:Unencrypted uploads,SSE-S3 uploads,Exam clue
> “Ensure all uploaded objects are encrypted with KMS”

4️⃣ Enforce specific KMS key
Condition
```json
"Condition": {
  "StringEquals": {
    "s3:x-amz-server-side-encryption-aws-kms-key-id":
    "arn:aws:kms:region:account-id:key/key-id"
  }
}
```
Meaning:Forces usage of one specific CMK
Exam clue:> “Use only customer-managed KMS key”

5️⃣ Deny public access
Condition
```json
"Condition": {
  "StringEquals": {
    "aws:PrincipalType": "Anonymous"
  }
}
(Usually paired with Effect: Deny)
Meaning: Blocks anonymous/public users
Exam clue> “Prevent public access to bucket”

6️⃣ Restrict access to a specific VPC Endpoint
Condition

"Condition": {
  "StringEquals": {
    "aws:sourceVpce": "vpce-123456"
  }
}
```
Meaning:Access allowed only via VPC endpoint,No internet access
Exam clue:> “Private access from VPC only”

7️⃣ Restrict by AWS account or organization
```json

"Condition": {
  "StringEquals": {
    "aws:PrincipalAccount": "123456789012"
  }
}

OR

"Condition": {
  "StringEquals": {
    "aws:PrincipalOrgID": "o-abc123xyz"
  }
}
```
Meaning
Only trusted accounts / org can access

Exam clue

> “Allow access only from same AWS Organization”

8️⃣ MFA-protected access (rare but exam-valid)

```json
"Condition": {
  "Bool": {
    "aws:MultiFactorAuthPresent": "true"
  }
}
```
Meaning
Requires MFA to access S3

Exam clue
> “Sensitive data, MFA required”
🆚 QUICK EXAM CHEAT TABLE

Requirement in Question	Condition Key

HTTPS only	aws:SecureTransport
Corporate IP	aws:SourceIp
Enforce SSE-KMS	s3:x-amz-server-side-encryption
Enforce CMK	s3:x-amz-server-side-encryption-aws-kms-key-id
VPC-only access	aws:sourceVpce
Org-only access	aws:PrincipalOrgID
MFA required	aws:MultiFactorAuthPresent

🧠 Exam Golden Rule (VERY IMPORTANT)
> Security enforcement = DENY with conditions
Most strong bucket policies:
Use Effect: Deny
Then apply conditions

Why?
👉 Explicit deny always wins

🧠 One-Line Exam Answer
> Advanced S3 bucket policy conditions control when access is allowed, such as enforcing HTTPS, IP restrictions, KMS encryption, VPC endpoints, and MFA.
