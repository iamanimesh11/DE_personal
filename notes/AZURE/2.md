# spark setting

fabric spark= no cluster management,workspace-level,capacity-billed

sessions end when notebook stops, can override workspace defaults
* ❌ spark session not shared across worskpace

> 1 workspace =1 managed spark pool


spark worspace seting control:
* executer size
* dynamic allocation
* session timeout
* default spark config

> Global spark behavior -> workspace settings


## executers

executer -worker process:
* managed automatcially,count and size are controlled by workspace spark settings
* billed to capacity

> workspace = behaivor ,logic =notebook



## Spark Structured Streaming ..

microbatch processing model 
* write results to delta lake
* append oriented ,fault tolerant,stateful via checkpoints

  > streeaming in fabric =micro batches +delta +checkpoints
  > check point stored in  onelake

### ignorechanges

tells spark if underlying delta file change,do not fail the stream
* it prevents failures


### ignore delete 

* ignore deleeted rows ,prevent stream failure due to deletes


>updates > ignroechangs ,deletes -> ignoredeeltes


```
---
```

### EXACTLY ONCE=checkpoit+delta

if checkpoint is removed,overwrie mode is used
