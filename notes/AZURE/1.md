Fabric is Saas first,worspace driven ,cpaacity absed billing,ne sotrage layer (onelake)

# architecture

```python 
S3 : Onelake base for everything
Glue,athena,redshift read from s3 : all fabric worload read from onelake
separate services : single workspace driven SaaS
IAM _lake formation : Fabric workspace security

```

fabric -> one workspace +one lake + one capacity

* onelake = storage
* shortcut= no copy access
* workspace = security boundary
* capacity = cost boundary

# Phase 2 akehouse & data engineering core.

delta tables provide 
* acid transactions
* time travel
* schema enforcemen

makes enable reliable analytics on data stored in lake

 

  spark-> tranform data,data science

  sql endpoint -> read data,bi queries
 

  ## Medallion architecutre

  > Raw -> cleaned -> curated in S3
  > Bronze = Raw
  > Silver =cleaned
  > gold = aggregrated

Data quality layers -? medallion arch in lakehouse

 
## security ,governance

```
access -> workspace RBAC
row filtering -> RLS
Compilance -> sensitivity label
tracking -> lineage
cost and performance -> capacity monitoring
```


```
who can see data -> RBAC (role based access control)
which rows user sees -> RLS
how sensitive data is -> sensitivity labels
```



# notes

*  Fabric administration is centralized in the Admin portal.
*  In Workspace settings, you can configure:

1. License type to use Fabric features.
2 .OneDrive access for the workspace.
3 .Azure Data Lake Gen2 Storage connection.
4. Git integration for version control.
5 .Spark workload settings for performance optimization.


```
item event-> trigger automation
diagnozse failure- > logs
monitro health -> metrics
```

Real time Hub -> shows eventstream,kql db,real time sources,schema and connection info
 ```
answes is who is ingesting,stauts of ingestion

```

>  FAIL -> EVENT -> MESSAGE -> MERIT -> LINEAGE
