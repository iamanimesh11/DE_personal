with cte as (
select p.product_id ,p.category,p.price,o.quantity,o.discount,
price*quantity*(1-o.discount/100) as revenue from
products p 
 join order_items o 
on p.product_id=o.product_id
)

select category,sum(revenue)
from cte 
group by category

